import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  backButtonContainer: {
    zIndex: 99,
  },
  backButtonImage: {
    height: 30,
    position: 'absolute',
    right: 8,
    top: 10,
    width: 35,
  },
  backButton: {
    backgroundColor: '#fff',
    borderRadius: 25,
    height: 50,
    left: 20,
    position: 'absolute',
    top: 40,
    width: 50,
  },
});
